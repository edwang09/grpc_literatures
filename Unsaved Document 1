grpc_tools_node_protoc --proto_path=../../protos --js_out=import_style=commonjs,binary:./practice/ --grpc_out=grpc_js:./practice/ helloworld.proto

DOCKER DEV

sudo docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build

DOCKER PROD
cp protos gRPCclient -r
cp protos gRPCserver -r
sudo docker-compose -f docker-compose.yml -f docker-compose.prod.yml build

sudo docker-compose -f docker-compose.yml -f docker-compose.prod.yml push


HOW TO INSTALL DOCKER

sudo apt-get update
sudo apt-get install \
    apt-transport-https \
    ca-certificates \
    curl \
    gnupg \
    lsb-release -y

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo \
  "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io
sudo curl -L "https://github.com/docker/compose/releases/download/1.29.1/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
export XXX=XXX

//to fix docker login issue
sudo apt install gnupg2 pass


GO SERVER

PREREQUISITE
Go, Protol buffer compiler, Go plugins for proto complier

protoc compile
export PATH=$PATH:/usr/local/go/bin
export PATH="$PATH:$(go env GOPATH)/bin"
export PATH="$PATH:$(go env GOPATH)/bin"
./protoc --go_out=./gRPCserver_go  --go_opt=paths=source_relative \
    --go-grpc_out=./gRPCserver_go --go-grpc_opt=paths=source_relative \
    literature/literature.proto
